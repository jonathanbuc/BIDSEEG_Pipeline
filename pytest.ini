[pytest]
# Run the fast suite with:   pytest -m "not slow"
# Run everything (incl. the end-to-end pipeline smoke test) with:   pytest
# Tests must be run inside the `mne-env` conda environment.
markers =
    slow: end-to-end runs that execute pipeline modules on the demo data (minutes).
testpaths = tests
